# Test Codes

This repository contains simple code examples and assets.

## 3D Character

A blocky character demo written in Babylon.js is provided in `index.html`.
Run a local web server in this directory and open the page to view the
character from a third-person perspective. A basic OBJ version of the
model is still available in `character.obj`.

